package streams;

public class Filter {
    public static void main(String[] args) {
        //Collection: to represent group of data/objects as single entity.
        //Stream : to process the data from collection we use streams concepts.
        /* A stream is a sequence of objects that supports various methods which
        can be pipelined to produce the desired result
        The whole idea of Java streams is to enable functional-style operations on
        streams of elements. A stream is an abstraction of a non-mutable collection
        of functions applied in some order to the data. A stream is not a collection
        where you can store elements.*/





    }
}
